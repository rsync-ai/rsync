# Requirements for MongoDB connector
#
# Base dependencies (always required)
# Note: base_connector.py is expected from the shared location (see Dockerfile).

# HTTP server for Docker deployment
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# MongoDB driver (pure-Python; no system package required). pymongo 4.x supports
# replica sets, mongodb+srv:// (Atlas), and change streams. The connector uses it
# for connectivity test, collection discovery, and batch export; CDC capture is
# performed by Debezium (change streams), not this connector.
pymongo>=4.6.0
