# Requirements for the BigQuery MCP connector (hand-curated).
# base_connector.py + warehouse_adapters.py are provided at build time
# (see Dockerfile: shared BuildKit context + PYTHONPATH).

# HTTP server for Docker deployment
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# Connector-specific dependencies
google-cloud-bigquery>=3.0.0
