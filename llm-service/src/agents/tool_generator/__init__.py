"""
Tool Generator agent package.

This file makes `agents.tool_generator.*` a regular Python package (instead of a
namespace package), which is more reliable under pytest's import/rewrite hooks.
"""

